
# Main config: choose RL algorithm/env/network/training via defaults.
defaults:
  - common: default
  - rl/agent: dqn
  - rl/env: cartpole
  - rl/network: mlp_small
  - rl/training: cartpole_fast
  - rl/eval: default 
  - _self_

# Top-level switch (rl or marl). For now we only use rl.
mode: rl
