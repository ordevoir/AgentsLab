# Master Hydra config
defaults:
  - common/env: cartpole
  - common/logging: default
  - common/train: base
  - common/eval: base
  - rl: reinforce
  - _self_

# You can override any field via CLI, e.g. rl=ppo, common.train.total_timesteps=100000